<%= button_to "Discover Page", discover_show_path(@user), method: :get %><br>

<div id="topmovies">
  <% @movies.each do |movie| %>
    <%= link_to movie.title, movie_show_path(@user, movie.id) %> Vote Average: <%= movie.vote_average %><br>
  <% end %>
</div>
