<h1>Discover Movies</h1>

<%= button_to "Find Top Rated Movies", "/user/#{@user.id}/movies" %><br>

<%= form_with url: "/user/#{@user.id}/movies", local: true do |form| %>
  <%= form.text_field :movie_title, placeholder: "Search by movie title" %>
  <%= form.submit "Find Movies"%>
<% end %>
