<h2> <%= @user.name %>'s Dashboard</h2>

<%= button_to "Discover Movies", discover_show_path(@user), method: :get %>

<hr>
<h3>Viewing Parties</h3>
<% @parties.each do |party| %>
  <%= party.name %>
  <%= party.date %>
  <%= party.time %>
<% end %>
